<UserControl x:Class="Patrones_Estructurales.Decorator.Decorator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,15">
            <Button Content="Validaciones dinámicas"
                    Style="{StaticResource MenuButtonStyle}"
                    Margin="5"
                    Click="BtnValidaciones_Click"/>

            <Button Content="Botones y TextBox"
                    Style="{StaticResource MenuButtonStyle}"
                    Margin="5"
                    Click="BtnControles_Click"/>

            <Button Content="Clase base"
                    Style="{StaticResource MenuButtonStyle}"
                    Margin="5"
                    Click="BtnBase_Click"/>
        </UniformGrid>

        <Border Grid.Row="1"
                CornerRadius="8"
                Padding="15">

            <ContentControl x:Name="ContenedorDecorator"/>

        </Border>
    </Grid>
</UserControl>