<UserControl x:Class="Patrones_Estructurales.Flyweight.EditorTexto.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Patrones_Estructurales.Flyweight.EditorTexto"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="850">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Flyweight: Editor de Texto" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextoOscuro}"/>
            <TextBlock Text="Escribe miles de letras. El sistema reciclará los caracteres para ahorrar memoria." Foreground="{StaticResource TextoClaro}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,15">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">

                <StackPanel Orientation="Horizontal" Margin="0,0,20,5">
                    <StackPanel Margin="0,0,10,0">
                        <TextBlock Text="Fuente:" FontWeight="Bold" FontSize="11"/>
                        <ComboBox x:Name="CmbFuente" Width="120" Margin="0,3,0,0">
                            <ComboBoxItem Content="Arial" IsSelected="True"/>
                            <ComboBoxItem Content="Consolas"/>
                            <ComboBoxItem Content="Times New Roman"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,0,10,0">
                        <TextBlock Text="Tamaño:" FontWeight="Bold" FontSize="11"/>
                        <ComboBox x:Name="CmbTamaño" Width="55" Margin="0,3,0,0">
                            <ComboBoxItem Content="14"/>
                            <ComboBoxItem Content="18" IsSelected="True"/>
                            <ComboBoxItem Content="24"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,20,5" VerticalAlignment="Bottom">
                    <TextBox x:Name="TxtEntrada" Width="130" Height="28" Margin="0,0,5,0" VerticalContentAlignment="Center" Tag="Escribe aquí..."/>
                    <Button x:Name="BtnEscribir" Content="Escribir" Width="75" Height="32" Style="{StaticResource MenuButtonStyle}" Click="BtnEscribir_Click"/>
                    <Button x:Name="BtnGenerar10k" Content="+10,000 Aleatorios" Width="135" Height="32" Margin="5,0,0,0" Style="{StaticResource MenuButtonStyle}" Click="BtnGenerar10k_Click"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5" VerticalAlignment="Bottom">
                    <Border Background="#F0F0F0" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                        <TextBlock x:Name="TxtStats" Text="Letras: 0 | Flyweights: 0" FontWeight="Bold" Foreground="{StaticResource ColorPrimario}" VerticalAlignment="Center" FontSize="12"/>
                    </Border>
                    <Button x:Name="BtnLimpiar" Content="Limpiar" Width="75" Height="32" Background="#E74C3C" Style="{StaticResource MenuButtonStyle}" Click="BtnLimpiar_Click"/>
                </StackPanel>

            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Background="White" BorderBrush="#CCC" BorderThickness="1" CornerRadius="3">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <Canvas x:Name="CanvasEditor" Background="White" Width="2000" Height="500"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>