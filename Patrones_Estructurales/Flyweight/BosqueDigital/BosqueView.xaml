<UserControl x:Class="Patrones_Estructurales.Flyweight.BosqueDigital.BosqueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Patrones_Estructurales.Flyweight.BosqueDigital"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Flyweight: Bosque Digital" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextoOscuro}"/>
            <TextBlock Text="Crea miles de árboles compartiendo solo unos pocos objetos en memoria." Foreground="{StaticResource TextoClaro}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Tipo de Árbol:" FontWeight="Bold"/>
                    <ComboBox x:Name="CmbTipos" Width="120" Margin="0,5,0,0">
                        <ComboBoxItem Content="Roble" IsSelected="True"/>
                        <ComboBoxItem Content="Pino"/>
                        <ComboBoxItem Content="Cerezo"/>
                    </ComboBox>
                </StackPanel>

                <Button x:Name="BtnPlantarUno" Content="Plantar 1" Width="100" Style="{StaticResource MenuButtonStyle}" Click="BtnPlantarUno_Click" Margin="5"/>
                <Button x:Name="BtnPlantarMil" Content="Plantar 1,000" Width="100" Style="{StaticResource MenuButtonStyle}" Click="BtnPlantarMil_Click" Margin="5"/>
                <Button x:Name="BtnLimpiar" Content="Reiniciar Bosque" Width="120" Style="{StaticResource MenuButtonStyle}" Background="#E74C3C" Click="BtnLimpiar_Click" Margin="5"/>

                <StackPanel VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock x:Name="TxtStats" Text="Árboles: 0 | Objetos en Memoria: 0" FontWeight="Bold" Foreground="{StaticResource ColorPrimario}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" BorderBrush="#DDD" BorderThickness="1" ClipToBounds="True">
            <Canvas x:Name="CanvasBosque" Background="#F0F9F0"/>
        </Border>
    </Grid>
</UserControl>